{
  "name": "portfolio",
  "version": "1.0.0",
  "repository": "https://github.com/Perchusha/Portfolio.git",
  "author": "Anton Perch",
  "license": "MIT",
  "scripts": {
    "prepare": "yarn --cwd client && yarn --cwd server",
    "start:client": "yarn --cwd client start",
    "start:server": "yarn --cwd server start:dev"
  }
}
